Run this command first to install the required libraries with your python

python -m pip install -r requirements.txt

After that, just open a cmd window in the directory and run
python nyaabag.py

Follow the instructions.